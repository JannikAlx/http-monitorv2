[package]
name = "http-monitor-common"
version = "0.1.0"
edition = "2021"

[features]
default = []
user = ["aya"]

[dependencies]
aya = { version = "0.12", optional = true }
aya-ebpf-bindings = "0.1.0"
aya-ebpf = "0.1.0"
[lib]
path = "src/lib.rs"
